# revision-app — frontend

Application Next.js (App Router) de [revision-app](../README.md).

```bash
pnpm install
pnpm dev   # http://localhost:3000 — nécessite le backend sur :8080
```

Voir le [README racine](../README.md) pour le démarrage complet (BDD, backend, Docker).
