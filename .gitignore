# --- secrets / env ---
.env
*.local

# --- rust ---
**/target/

# --- node / next.js ---
node_modules/
.next/
out/
.turbo/

# --- misc ---
.DS_Store
*.log
Thumbs.db

# NOTE: backend/.sqlx/ is intentionally NOT ignored — the offline query
# cache must be committed so Docker builds without a live database.
